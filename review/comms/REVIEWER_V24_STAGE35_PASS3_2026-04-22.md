RED-TEAM REVIEW PASS #3 — Stage 3.5 v2 + v2.2 amended

VERDICT: REWORK

NEW CRITICAL findings (silent failure modes, v2.3.2-class risks):

- **[CRIT-P3-1] Multiway cache contract silently cross-contaminates per-villain ranges.**
  Amended v2.2 §3.7 lines 649-650: `if cached_range is not None: return cached_range, cached_meta or {}`. Equity call site passes `cached_range=_cached_vr` (the single primary-villain range from `_villain_range_narrowed`). In multiway, equity MC loop needs per-villain ranges, but spec lines 686 `per_villain_ranges = {}` is never exposed in the helper's declared return type (`Tuple[Dict[str, float], Dict]`, line 641). Line 718 comment says "this helper returns both via meta" — undocumented, untyped magic. If equity passes the primary's cached_range while looping over non-primary villains, helper short-circuits and returns primary's range for every villain. Silent cross-contamination of villain identity into equity calc. Spec is incomplete: no multiway cache key contract, no per-villain cache dict spec.

- **[CRIT-P3-2] Bi-schema helper returns non-binary int, violating documented contract (amended §3.2 line 372).**
  `return int(val) if val else 0` — for `val=2` returns `2`; for `val=0.5` returns `0` (silent truncation). Function docstring (line 325, 330) declares "Returns binary 0/1." Trainer or downstream that expects strict 0/1 sees bogus values. No defensive clamp. Also case-sensitive string match (line 371): `val in ('PRIMARY', 'CONFIRMED')` — labeller emits `"primary"`, `" PRIMARY "`, `"Confirmed"` → all silently coerced to 0 (attention lost). The labeller prompt v3.2 hasn't shipped, so case/whitespace contract is unproven.

- **[CRIT-P3-3] Commit-1 tuple-unpack scope mismatch crashes coaching/explain_hand.py.**
  Blueprint v2 §2.1 line 290-295 enumerates "Root sites (12 edits + 5 test-file edits)" — does NOT include `coaching/explain_hand.py:264, 329`. §2.1 line 298 defers "Coaching sites (9 edits) — deferred to MUST #8 partial-delete commit." But amended §1.1 line 90 AND commit table line 544 say coaching/explain_hand.py:264, 329 ARE in commit 1. `coaching/explain_hand.py:261, 326` explicitly `from range_narrowing import narrow_to_betting_range` (NOT from coaching/range_narrowing). So after commit 1 patches root `range_narrowing.py` to tuple-return, and if the §2.1 list is implemented literally, lines 264/329 silently do `v_range = (dict, mass_tuple)` — v_range becomes a tuple, downstream dict ops crash. **Live teaching crash window: commits 1→5 (≈ 4 commits).** Spec inconsistency between blueprint v2 §2.1 and amended §1.1/commit table must be reconciled before ship.

- **[CRIT-P3-4] Audit column `v2.4_55flag` mislabels a 54-flag CSV and silently mis-tags the held-back→un-held-back transition.**
  Amended §3.2 line 376: `ATTENTION_VOCAB_VERSION = f"v2.4_{len(FEATURE_COLUMNS)}flag"`. `len(FEATURE_COLUMNS)` = 55, but RAW_FEATURES strips `board_adjusted_hrp` → 54. **Day-1 mismatch: CSV has 54 attn flags, audit column says `v2.4_55flag`.** Worse, when Stage 5 un-holds-back (toggling `_HELD_BACK_FEATURES` to `{}`), `len(FEATURE_COLUMNS)` stays 55, RAW_FEATURES becomes 55 — audit column still reads `v2.4_55flag`. **Two different CSVs (54 cols pre-Stage-5, 55 cols post-Stage-5) share the same audit tag.** Trainer MUST #36 check (amended §3.4 line 506-513) only detects MISSING columns; extra columns pass silently. Trainer with 54-col code silently consumes 55-col CSV → up-training with wrong width. This is a literal vocabulary-drift silent failure.

NEW HIGH findings:

- **[HIGH-P3-5] Silent fallback paths missed in MUST #9 pipeline unswallow.**
  `review/recovered/eval_FB40_plus_ablation.py:84` and `:123` have blanket `except Exception as e:` wrapping `extract_all_features(hand_dict)`. `eval_FB40_attn_per_feature.py` and `eval_MW_with_legal_action_masking.py` call `extract_all_features` with bare-except modules loaded. Blueprint MUST #9 scope lists only `extract_features_parallel.py:81`, `extract_incremental.py:105`, `gauntlet_v5_37feat.py:188,226`. If any ablation/eval re-run fires post-CRIT-#2, silent re-labelling happens — same v2.3.2-class pattern. Recovered/ scripts are not deprecated, just shelved.

- **[HIGH-P3-6] MUST #32(a) "manifest-gate lockout" is documentation, not tooling.**
  Amended §3.5 line 538: "manifest-level enforcement — Stage 4 cannot fire while Stage 3.5 implementation is in flight. Manifest v1.10 already has this gate; amendment strengthens with explicit 'no re-label while MUSTs-in-flight' language." A YAML doc does not gate `python3 run_oracle_labeller.py`. A parallel-terminal builder with default env=warn can ship silent-fallback labels during the entire commit 2 → commit 16 window (14+ commits). No pre-commit hook, no CI gate, no runtime check blocks this. The manifest-gate is a convention, not a lockout.

- **[HIGH-P3-7] Sidecar validator lacks poker-semantic well-formedness check.**
  Amended §3.8 lines 784-785: validator checks "actions are in {RAISE, CALL, FOLD, CHECK, BET} (plus check-raise as CHECK + RAISE pair on same street)". No cross-check that CALL is preceded by an outstanding bet/raise on the same street. 140 hand-authored sidecar entries × human error probability → near-certain that at least one entry is syntactically well-formed but poker-nonsense (e.g., villain "CALLs" an unraised pot — which narrow_to_continuing_range would process as if there were a bet to call). BET vs RAISE confusion (authoring `BET` when prior aggression exists) routes through `narrow_to_betting_range` instead of raise-narrowing, distorting composition. Validator passes, chain produces wrong range, silent label drift.

- **[HIGH-P3-8] Commit 4 (merged CRIT #1 + MUST #10) does NOT include equity chain-inheritance — window persists.**
  Amended §3.5 line 547 merges CRIT #1 + HIGH #4 + MUST #10 + MUST #15 + MUST #28 into commit 4. But commit 5 (line 548) still separately lands "Equity + explain_hand + coaching/explain_hand chain inheritance (MUST #6 + #19 + #30)." Between commit 4 and commit 5: composition returns NaN sentinel on folded/overflow, but equity still uses bypass path → composition-equity label mismatch (composition=NaN, equity=bypass-value). Mid-deploy seam the merge was supposed to eliminate is only partially closed. Commit 4 and 5 should further merge, OR composition should not NaN-signal until commit 5 lands too.

- **[HIGH-P3-9] MUST #34 multiway spec has inconsistent action-history normalization.**
  Amended §3.7 lines 691-694 uses `_normalize_action_entry(e).get('position', '').upper()` for position filter, but line 696 uses raw `e.get('street', '').lower()` for the post-flop filter. If `action_history` contains tuple-shaped entries (not dict), raw `e.get()` throws `AttributeError`; normalized path works. Mixed normalization → crash on one entry type, pass on the other — silent per-hand divergence.

NEW MEDIUM findings:

- **[MED-P3-10] Blueprint v2 line 942 still contains `raise NotImplementedError` for multiway branch.** Amended §3.7 supplies the spec, but blueprint v2 has not been patched. Builder could implement from blueprint v2 as-written and land `raise NotImplementedError` into production. Resolution requires explicit "amended §3.7 supersedes blueprint v2 §11" marker, or blueprint v2 re-cut.

- **[MED-P3-11] ASSEMBLER_PATTERN.md enforcement is "Stage 4 review gates on conformance" — no lint/CI.**  Reviewer must manually verify Stage 4's future `assemble_v2_4.py` imports `_attention_value`, writes audit column, uses correct `RAW_FEATURES`. No `test_assembler_conformance.py`, no import-shape check. Past pattern: audit-column omission in a clone would pass review unless reviewer specifically looks.

- **[MED-P3-12] CONTENT_API v4 ticket §5 ship-gate is a manual ping.**
  Ticket §5 line 196-197: "Teaching terminal pings orchestrator: 'CONTENT_API v4 ready' / Orchestrator gates Stage 3.5 commit 4 on this ping." Orchestrator takes teaching's word. Teaching could ship 3 of 5 test cases (§3.1-§3.5) passing and ping ready. No independent gate that all 5 test cases exist and pass. Commit 4 unblocks on trust.

ATTACK RESULTS:
- MUST #32(a) manifest-gate enforcement gap: documentation only; any parallel-terminal `python3` invocation with default env=warn bypasses the gate for the 14+ commit implementation window. Recommendation: pre-commit hook or runtime `STAGE4_STRICT_ACTION_HISTORY` assertion in `assemble_v2_4.py` entry point that aborts unless env=raise.
- MUST #32(b) merged commit internal bug risk: merge bundles CRIT #1 + HIGH #4 + MUST #10 + #15 + #28 — 5 concerns in one commit. If any one has a latent bug (e.g., folded-villain sentinel at wrong layer), bisect identifies "commit 4" with 5 concurrent changes. Review burden concentrated; per-MUST reviewer checklist needed. Also does NOT cover equity chain-inheritance (commit 5) — seam persists (HIGH-P3-8).
- MUST #34(b) cache cross-contamination risk: confirmed CRIT-P3-1. Cache contract unspecified for multiway (helper returns single range, equity MC loop needs per-villain dict). Silent primary-range leakage across villains.
- Bi-schema helper detection failure modes: mixed-batch OK (per-record detection via line 346 short-circuit-or). BOTH keys present → silently prefers `feature_attention`, drops pilot-style `attention_flags`. Neither key → all returns 0 (legacy). Non-PRIMARY/CONFIRMED string → 0. val=2 → returns 2 (contract violation, CRIT-P3-2). val=None → 0.
- Q38 most catastrophic misinterpretation path: `val=2` or `val=0.5` returning non-0/1 int (CRIT-P3-2); case-variation `"primary"` returning 0 silently → entire attention-flag signal zeroed without raising. Training proceeds with all-zero attention → equivalent to disabled attention head → model learns raw features only → silent regression relative to design intent. No strict-mode trip because mandatory flag was PRESENT (just mis-cased) — misses the raise entirely.
- board_adjusted_hrp transition attack: CRIT-P3-4. Audit column uses `len(FEATURE_COLUMNS)` (constant 55) not `len(active_features)` (54 pre, 55 post-transition). Same tag for two CSV widths. Trainer `missing` check doesn't catch the 54→55 extra-column transition.
- Sidecar validator-miss failure modes: HIGH-P3-7. Syntactic-but-poker-nonsense sequences (CALL without prior bet, BET when raise, off-order streets masquerading as monotonic via subtle list order). 140 entries × human error → high.
- 16-commit sequence longest poisoning window: commits 2 → 16 for STAGE4_STRICT_ACTION_HISTORY default-warn (14 commits, ~days). Commits 1 → 5 for coaching/explain_hand.py tuple-crash (CRIT-P3-3). Commit 4 → 5 for composition-NaN/equity-bypass mismatch (HIGH-P3-8).
- docs/ASSEMBLER_PATTERN.md enforcement attack: MED-P3-11. No tooling. Stage 4 clone reviewer could miss audit column, miss strict-env import, miss held-back filter. Pattern doc drift likely over time. Minimum-viable fix: `test_assembler_conformance.py` imports every known assembler and asserts each has `_attention_value`, `_attention_vocabulary_version` audit column, and `ASSEMBLER_STRICT_ATTENTION` env handling.
- CONTENT_API v4 partial-coverage gate: MED-P3-12. Ticket §5 trusts teaching's ping. No requirement that the 5 test cases live in `teaching/tests/test_content_api_v4_nan_render.py` and all pass green. Orchestrator gate should require a specific test artifact, not a ping.

MANDATORY CHECKS:
- NotImplementedError/TODO/placeholder grep: **NOT CLEAN.** `/tmp/blueprint_v2.md:942` still contains `raise NotImplementedError  # placeholder for blueprint`. Amended supplement §3.7 replaces it textually but blueprint v2 is not patched. `/tmp/ticket_v4.md:107, 114, 205` have "placeholder" references (benign — blocker-flag placeholder, Q2 is a clarifier).
- Bypass-site count: 14 call-site tuple-unpacks confirmed across feature_extractor.py (6) + explain_hand.py (2) + coaching/feature_extractor.py (5 — deleted at commit 9) + coaching/explain_hand.py (2) + range_narrowing.py internals (3 at 791/794/797) + 2 test-demo at 875/885. Plus 5 test-file edits. Matches amended §1.1 total "14 non-deleted root+coaching sites + 5 test-file sites." No NEW bypass sites surfaced.
- Silent-fallback hunt: **2 new.** `review/recovered/eval_FB40_plus_ablation.py:84 + :123` blanket except (HIGH-P3-5). Multiway cache contract silent primary-leak (CRIT-P3-1). Bi-schema helper silent case-variation-to-0 (CRIT-P3-2).

OTHER RED-TEAM FINDINGS:

- **Spec inconsistency:** blueprint v2 line 1728 says "13 call-site tuple-unpacks" while amended §1.1 says 14 and commit table line 544 says 14. Reconcile to single canonical count. Builder implementing from blueprint v2 will miss the 14th.
- **Blueprint v2 line 942 NotImplementedError**: will land in commit 5 output if builder copy-pastes the blueprint skeleton. The amended spec at §3.7 lines 680-721 is an "Implementation sketch" with `merged = {}` dead-variable — not a ship-ready implementation.
- **Blueprint §2.1 line 290 "coaching sites (9 edits) — deferred to MUST #8 partial-delete"** suggests coaching/explain_hand.py is NOT touched until commit 9. Conflicts with amended §1.1 line 90 (commit 1). Must be fixed in spec.
- **Amended §3.2 line 346** `fa = record.get('feature_attention') or record.get('attention_flags', {}) or {}` — if `feature_attention={}` (empty dict, truthy-False), falls through to `attention_flags`. Accidentally correct for empty production schema, but semantically ambiguous (was it empty or missing?). Mandatory-tag strict check fires only on per-key miss, not on empty dict globally. Labeller emitting `feature_attention: {}` for every record passes (every mandatory key missing strictly, but in practice the strict warn message fires per-feature and floods logs, not a global abort).
- **Commit 16 "Path (c) Phase 2 sidecars (owner-gated) + Stage 3.5 SHIP"** — combining sidecar-authoring + ship flag in one commit conflates two distinct decisions. Owner-gated sidecar should land in its own commit; ship flag separate.

Hostile summary: merging commits 4+5 partially closed the seam but did not cover equity (HIGH-P3-8). Bi-schema helper has contract-violating int passthrough and case-sensitivity silent-failure (CRIT-P3-2). Multiway cache is underspec'd and can silently leak primary-villain's range across MC trials (CRIT-P3-1). Held-back→un-held-back audit column uses wrong `len()` → day-1 mismatch and silent vocab-drift at Stage 5 (CRIT-P3-4). Coaching/explain_hand.py tuple-crash window unresolved between blueprint v2 §2.1 and amended §1.1 (CRIT-P3-3). Manifest-gate and pattern-doc enforcement are both trust-the-human, not tool-gated (HIGH-P3-6, MED-P3-11). Builder cannot ship with these resolutions pending.

REWORK required before pass #4.
